# Getting Started

## Installation

1. Download the latest Windows installer from [GitHub Releases](https://github.com/Cyrene2008/AudioSeeker/releases).
2. Run `AudioSeeker_<version>_x64-setup.exe`.
3. Let the first-launch bootstrap prepare the runtime (Python, dependencies, FFmpeg).

## First Use

1. Open **Build Index**, select a folder containing audio (WAV, FLAC, MP3, OGG, AAC, M4A, etc.).
2. Enter an index name and optionally set worker count and target segment memory (MB).
3. Start the build and wait for it to finish.
4. Open **Search**, choose the index and a sample, then start matching.
5. Play, favorite, or export the results.

## Data Locations

- Default index directory: the `index` folder beside the installed application.
- Settings, favorites, history, and index registry: the current user's application-data directory.

## Related Links

- [GitHub Repository](https://github.com/Cyrene2008/AudioSeeker)
